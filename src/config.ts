export const Config = {
  // Camera settings
  camera: {
    fov: 75,
    near: 0.1,
    far: 1000,
  },
  
  // Renderer settings
  renderer: {
    clearColor: 0x000000,
    shadowMapEnabled: true,
  },
  
  // Player settings
  player: {
    moveSpeed: 5,
    rotationSpeed: 0.1,
  },
  
  // Game settings
  game: {
    gravity: 9.8,
    debug: true,
  },
  
  // UI settings
  ui: {
    fadeTime: 0.5,
  },
  
  // Mobile settings
  mobile: {
    controlSensitivity: 0.5,
  },
}; 